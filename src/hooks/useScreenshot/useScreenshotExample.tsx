// import useScreenshot from "./useScreenshot";

// const MyComponent = () => {
//   const screenshot = useScreenshot();

//   return (
//     <>
//       {screenshot && (
//         <img src={URL.createObjectURL(screenshot)} alt="Screenshot" />
//       )}
//       <p>Scroll down to generate a full-page screenshot</p>
//     </>
//   );
// };
